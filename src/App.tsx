import React from "react";
import AnnouncementEditor from "./lib";
import CEditor from "../lib";

console.log(CEditor);
const App = () => {
  return <AnnouncementEditor content={[]} attachment={[]} />;
};

export default App;
